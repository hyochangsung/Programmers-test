-- 동물보호소에 들어온 동물 이름 중 두 번 이상 쓰인 이름과
-- 해당 이름이 쓰인 횟수를 조회하시오.
-- 이름이 없는 동물은 집계에서 제외하며,
-- 결과는 이름 순으로 조회하시오.

SELECT ANIMAL_INS.`NAME`, COUNT(ANIMAL_INS.`NAME`) as COUNT
FROM ANIMAL_INS
GROUP BY ANIMAL_INS.`NAME`
HAVING COUNT(ANIMAL_INS.`NAME`) >= 2
ORDER BY ANIMAL_INS.`NAME`